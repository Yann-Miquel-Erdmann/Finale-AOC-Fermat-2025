( Implémentation des listes chainées d'entiers )
Soit init une fonction qui lorsqu'elle est appelée avec v exécute:
    Soit l La liste vide?.
    Ajouter à La liste l? La variable v?.
    Ajouter à La liste l? Un pointeur vers Rien??.
    Renvoyer La liste l?.
.

Soit ajouter une fonction qui lorsqu'elle est appelée avec p, m exécute:
    Soit l La valeur pointée par La variable p??.
    Si La valeur de La variable l? est égale à la valeur de Rien?? alors faire:
        Afficher La chaîne de caractères "La liste n'existe pas"?.
    sinon faire:
        Soit ptr L'élément de La liste l? à l'indice L'entier un??.
        Soit maillon La liste l?.
        Tant que La valeur de La variable ptr? est différente de la valeur de Rien?? faire:
            Que La variable maillon? devienne La valeur pointée par La variable ptr??.
            Que La variable ptr? devienne L'élément de La liste maillon? à l'indice L'entier un??.
        .
        Remplacer l'élément de La liste maillon? à l'indice L'entier un? par La variable m?.
    .
.

Soit afficher_liste une fonction qui lorsqu'elle est appelée avec l exécute:
    Si La valeur de La variable l? est égale à la valeur de Rien?? alors faire:
        Afficher Rien?.
    sinon faire:
        Afficher L'élément de La liste l? à l'indice L'entier zéro??.
        Soit ptr L'élément de La liste l? à l'indice L'entier un??.
        Tant que La valeur de La variable ptr? est différente de la valeur de Rien?? faire:
            Afficher L'élément de La valeur pointée par La variable ptr?? à l'indice L'entier zéro??.
            Que La variable ptr? devienne L'élément de La valeur pointée par La variable ptr?? à l'indice L'entier un??.
        .
    .
.

Soit premier_maillon Le résultat de la fonction init avec les arguments L'entier zéro??.
Soit deuxième_maillon Le résultat de la fonction init avec les arguments L'entier un??.
(Afficher La variable premier_maillon?.
Afficher La variable deuxième_maillon?.)

Soit z Un pointeur vers La variable deuxième_maillon??.

Exécuter la fonction ajouter avec les arguments Un pointeur vers La variable premier_maillon??, La variable z?.
(Afficher L'élément de La liste premier_maillon? à l'indice L'entier zéro??.

Afficher L'élément de La liste premier_maillon? à l'indice L'entier un??, La chaîne de caractères "_"?.)
Afficher La variable premier_maillon?.
Afficher L'élément de La variable premier_maillon? à l'indice L'entier un??.
(Afficher La valeur pointée par L'élément de La variable premier_maillon? à l'indice L'entier un???.
Exécuter la fonction afficher_liste avec les arguments La variable premier_maillon?.
Afficher La variable deuxième_maillon?.
Exécuter la fonction afficher_liste avec les arguments La variable deuxième_maillon?.)
